{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd mmf_backend && npm install && cd ../mmf_frontend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd mmf_backend && npm start",
    "restartPolicyType": "ON_FAILURE"
  }
}
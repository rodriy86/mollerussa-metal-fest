{
  "name": "mmf-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build:all": "cd frontend && npm run build",
    "start:app": "cd backend && npm start"
  }
}